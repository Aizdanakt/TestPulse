<tr>
  <td><%= student.name %></td>
  <td><%= student.group %></td>
  <td><%= student.teacher_id %></td>
  <td><%= link_to 'Show', admin_student_path(student), class: 'btn btn-primary' %></td>
  <td><%= link_to "Delete", admin_student_path(student), class: 'btn btn-danger', data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %></td>
</tr>
