<% if resource.present? && resource.errors.present? %>
  <%= content_tag :div, class: 'resource-errors alert alert-info', role: 'alert', data: { resource_id: resource.id } do %>
    <% resource.errors.each do |message| %>
      <p><%= message.full_message %></p>
    <% end %>
  <% end %>
<% end %>
